Command reference
=================

Generated from the CLI itself, so it cannot drift from the flags the code
actually accepts. Every command is also available as ``association <command>
--help``.

.. click:: association.cli:cli
   :prog: association
   :nested: full
