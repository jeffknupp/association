API reference
=============

Every module in the package, generated recursively at build time. Module
docstrings carry the design rationale — why a thing works the way it does, and
what went wrong when it did not — so they are worth reading before the
signatures.

.. autosummary::
   :toctree: generated
   :template: module.rst
   :recursive:

   association
